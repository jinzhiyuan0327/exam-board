# 考试看板 Exam Board

独立部署的考试大屏 + 管理后台，基于 React + Vite + Vercel Serverless。

## 功能
- 🖥️ **考试大屏**：全屏深色大时钟 + 实时倒计时 + 进度条 + 开考提醒
- ⚙️ **管理后台**：添加/编辑/排序/导入考试科目，一键同步到服务器
- 🕐 **网络校时**：自动通过 `/api/time` 校准时钟
- 🔔 **统一提醒**：五种设计各自匹配的全屏提醒浮层，支持自定义提醒
- 📢 **系统公告**：作者端统一发布 Markdown 公告；新实例自动展示，运行中检测到公告更新后自动弹出，五套大屏均可从顶栏随时查看
- 🛠️ **系统设置**：集中管理校时、默认设计、提醒与关于（内置 README）
- 📡 **实时同步**：大屏每 30 秒自动拉取最新考试数据

## 目录结构
```
├── api/                 # Vercel Serverless Functions
│   ├── exams.ts         # 考试数据读写
│   ├── login.ts         # 管理员登录
│   ├── time.ts          # 服务器时间
│   └── _auth.ts         # 鉴权工具
├── src/
│   ├── pages/           # 页面组件
│   │   ├── ExamPage.tsx     # 考试大屏
│   │   ├── AdminPage.tsx    # 管理后台
│   │   └── WelcomePage.tsx  # 首页
│   ├── hooks/           # React Hooks
│   ├── services/        # 接口服务
│   ├── utils/           # 工具函数（时间、校时、设置）
│   ├── styles/          # CSS 样式
│   └── types/           # TypeScript 类型
├── index.html
├── vite.config.ts
├── vercel.json
└── package.json
```

## 快速部署（Vercel，推荐：直接导入作者仓库）

> 推荐客户端部署时直接导入作者仓库 **`jinzhiyuan0327/exam-board`**（而非 Fork 到自己名下）。这样作者每次更新仓库时，Vercel 会自动重新构建部署，客户端无需任何 GitHub 操作，刷新网页即为新版本。

1. **登录 Vercel**（用 GitHub 账号登录，如首次使用需授权 Vercel 访问 GitHub）。
2. **新建项目**：Dashboard 右上角 **Add New… → Project**。
3. **导入作者仓库 `jinzhiyuan0327/exam-board`**：
   - 在 *Import Git Repository* 列表里找到 `jinzhiyuan0327/exam-board`，点 **Import**。
   - 若列表里没有（仓库不在你名下），点 *Import Third-Party Git Repository* / **Adjust GitHub App Permissions**，或直接粘贴仓库地址 `https://github.com/jinzhiyuan0327/exam-board` 导入。
   - **请勿 Fork 到自己仓库**；Fork 后作者的更新不会自动同步到你的 Fork。
4. **保持构建设置默认**：Framework 会自动识别为 **Vite**，Build Command / Output Directory 无需修改。
5. **设置环境变量（展开 Environment Variables，仅两个）**：
   - `DATABASE_URL` — Neon Postgres 连接串（必填）
   - `ADMIN_PASSWORD` — 管理端登录密码（必填；留空则免登录）
   - 其余变量（含 `TOKEN_SECRET`）均在部署时自动生成或使用内置默认，**无需填写**
6. **点 Deploy**，等待构建完成后访问分配的域名即可使用。

### 后续更新（自动）
作者向 `jinzhiyuan0327/exam-board` 的 `main` 分支 push 新代码后，所有导入了该仓库的 Vercel 项目会**自动重新部署**，客户端无需处理；约 1–3 分钟后刷新网页即为新版本。若你是 Fork 自行维护的客户端，则需自行合并更新，或在设置页「🚀 版本与更新」点「一键重新部署」（需先配置 `VERCEL_DEPLOY_HOOK_URL`）。

> 旧方式（自行维护代码时）：Fork 或上传本仓库到自己的 GitHub → 在 Vercel 导入自己的仓库 → 同样只需配 `DATABASE_URL` + `ADMIN_PASSWORD`。

## 本地开发

```bash
npm install
npm run dev
```

## 路由
- `/` — 首页（模式选择）
- `/exam` — 考试大屏
- `/admin` — 管理后台

## 数据持久化说明

> ⚠️ 当前 API 使用内存存储，Vercel 冷启动后数据会丢失。
> 生产环境建议接入 Vercel KV（Redis）或 Supabase 持久化数据。

## 导入考试数据格式

```json
[
  {
    "name": "语文",
    "startTime": "2026-06-07T09:00:00",
    "endTime": "2026-06-07T11:30:00",
    "enabled": true
  },
  {
    "name": "数学",
    "startTime": "2026-06-07T15:00:00",
    "endTime": "2026-06-07T17:00:00",
    "enabled": true
  }
]
```

## 使用遥测（匿名）

本应用内置匿名部署/运行遥测，用于作者统一掌握多方部署情况：

- **首次运行需同意**：首次打开会弹出数据收集须知，**不同意则无法使用**；同意后可随时在「系统设置 → 使用遥测」中暂停上报。
- **上报内容**：实例标识、版本、主机名、时区、语言、大致地区、匿名化 IP 哈希；**不含任何考试内容或个人身份信息**。
- **密钥内嵌**：收集器地址与上报密钥已内嵌在服务端函数 `api/telemetry.ts` 中（不会下发到浏览器）。**部署者仅需配置 `DATABASE_URL` 与 `ADMIN_PASSWORD`。**
- **数据流向**：浏览器 → 本实例 `/api/telemetry`（服务端补全地区/IP 哈希）→ 作者端收集器 `https://telemetry.pikachu2026.space/api/collect`。


## 环境变量（部署配置）

考试看板在 Vercel 部署时可配置以下环境变量：

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `DATABASE_URL` | 是 | Neon Postgres 连接串 |
| `ADMIN_PASSWORD` | 首次初始化建议填写 | 首次管理员密码。首次成功登录后会自动迁移为 Neon 内的安全密码哈希；后续可在设置页直接修改，无需重新部署 |
| `TOKEN_SECRET` | 否 | 无需客户端填写。首次初始化后，随机令牌密钥保存在客户端自己的 Neon 数据库中；密码修改会使旧令牌失效 |
| `VERCEL_DEPLOY_HOOK_URL` | 否 | Vercel Deploy Hook 地址；配置后可在「设置 → 版本与更新」一键拉取最新代码重新部署。生成路径：Vercel 项目 → Settings → Git → Deploy Hooks |
| `GITHUB_REPO` | 否 | 检查更新所用仓库，默认 `jinzhiyuan0327/exam-board`，格式 `owner/repo` |
| `GITHUB_TOKEN` | 否 | 提升 GitHub API 速率限制；私有仓库检查更新时必填 |

「检查更新」会读取 GitHub 仓库的最新 Release（无 Release 时回退到最大 tag），与当前构建版本做语义化版本比较；「一键拉取并重新部署」在配置了 `VERCEL_DEPLOY_HOOK_URL` 后触发 Vercel 从 GitHub 重新构建部署。

## v1.15.0：PWA、离线与安全更新

- **PWA 安装与离线打开**：首次联网访问后可添加到设备桌面；应用壳会缓存用于离线打开，但 `/api/exams`、公告、校时、登录及写入接口始终网络优先，不会用陈旧缓存覆盖云端数据。
- **Welcome 启动页**：从 PWA 图标启动时统一进入 Welcome；无考试且未安装时仅显示低存在感安装提示。无操作倒计时改为 10 秒并实时显示剩余秒数。
- **离线同步**：考试大屏在断网时使用本地考试快照和已保存的校时偏移继续走时；网络恢复、页面回到前台或正常轮询时会自动重新拉取云端数据。后台本地先保存，恢复联网后自动回推。
- **冲突保护**：考试保存请求携带云端版本；发现另一设备已更新时服务端返回冲突而非静默覆盖，本机修改会保留并提示管理员确认。
- **密码修改**：首次使用环境变量 `ADMIN_PASSWORD` ��������������录后会迁移为 Neon 数据库内的 scrypt 密码哈希。设置页可直接修改管理员密码，无需重新部署；修改后所有旧登录令牌失效。
- **公告保护**：考试进行中收到公告更新只暂存，考试结束提醒完成后再展示最新公告。

## v1.16.0：公告阅读与排序

- 公告统一按置顶分组优先、各组按更新时间从新到旧排列。
- 设置页、后台和考试大屏公告均支持逐条展开/收起。
- 公告阅读状态按本机浏览器保存；未读的最新公告版本显示 `NEW`，展开阅读后标记为已读。

## v1.16.1：手机端设置页滑动修复 + 移动端操作优化

### 滑动修复
- 设置页由「内层容器滚动」改为**文档自然滚动**，解决手机浏览器地址栏收起/软键盘弹出时设置页无法下滑、底部卡片被截断的问题。
- 移除全局 `body{overflow:hidden}`，改为按路由控制：仅考试大屏（`/exam`）通过 `body.lock-scroll` 锁定，其余页面均可自然滚动。
- 底部留白改为 `max(48px, env(safe-area-inset-bottom))`，避免全面屏手势条遮挡最后一张卡片。
- 密码等输入框聚焦时自动滚入可视区中央（仅手机端触发），软键盘弹出也够得着。

### 移动端操作优化（仅 ≤620px 生效，桌面端无变化）
- 输入框字号在手机端提升至 16px，防止 iOS Safari 聚焦时自动放大整页。
- 校时间隔输入加 `inputMode="numeric"` 唤起数字键盘（毫秒微调保留可输负号）。
- 按钮触摸目标 ≥44px，主操作按钮拉满宽度更易点。
- 窄屏内边距瘦身（页眉/卡片 padding 收紧）。
- 手机端「查看 README」改为页内展开；「新标签页打开」按钮仅桌面端显示。

### 布局调整
- 设置页「公告」模块上移至「关于」之上，「关于」置于页面最底部。

## v1.16.2：状态胶囊同步实时状态 + Welcome/黑板布局修复

- **状态胶囊反映“当下真实状态”（五个设计统一）**：新增实时在线检测（`navigator.onLine` + `online`/`offline` 事件），胶囊文案由“在线 + 校时”组合得出：
  - 在线且已校时 → **在线 · 已校时**（绿）
  - 在线但未校时 → **在线 · 未校时**（黄）
  - 离线 → **离线 · 本机时间**（红），不再因“历史校过一次”而永久绿灯。
  - 修复了本地 `npm run dev` 因代理校时成功而永远显示“在线·已校时”、断网也不变的问题。
- **Welcome 首页卡片文字对齐**：统一两张卡片 DOM 结构，新增 `.welcome-card__text` 纵向文字列，修复“考试大屏 / 全屏倒计时显示”挤在同一行的错位。
- **设计三（校园黑板）顶栏居中**：顶栏改为 `grid-template-columns:1fr auto 1fr` 三分区布局，标题无论右侧按钮多宽都真正居中（修复桌面端标题偏左）。

## v1.16.3：Neon 查询 TypeScript 类型兼容修复

- 修复 Vercel 使用 TypeScript 5.9 检查 API 时，`@neondatabase/serverless` 模板查询返回联合类型导致的 `TS7053` / `TS2339` 警告。
- 在 `api/_auth.ts` 的认证配置读取，以及 `api/exams.ts` 的考试读取、版本冲突写入和冲突回读中，明确约束默认「行数组」查询模式的结果类型。
- 不改变 SQL、鉴权、考试数据结构或运行时行为；仅收窄 SDK 的静态类型边界，避免部署日志出现 API 类型错误。

## v1.16.4：旧数据库时间戳自动迁移

- 修复早期部署的 `exam_data.updated_at` 列仍为 PostgreSQL `INTEGER` 时，写入毫秒级时间戳导致 `22003: out of range for type integer`、无法保存考试数据的问题。
- 新增一次性自动迁移：首次检测到该溢出时，自动执行 `updated_at INTEGER → BIGINT` 无损扩容，并自动重试本次保存；无须手动进入 Neon 执行 SQL。
- 按需建表/缺列修复路径也会确保 `updated_at` 为 `BIGINT`；正常实例不增加每次请求的 DDL 开销。
- 已核查作者遥测台：其事件、实例、公告与图片的毫秒时间字段自早期版本起均使用 `BIGINT`，不受此问题影响。

## v1.16.5：毫秒版本号 BIGINT 参数修复

- 修复 v1.16.4 自动迁移后，考试保存 SQL 中的 `baseUpdatedAt` 首次与字面量 `0` 比较时仍可能被 PostgreSQL 推断为 `INTEGER` 的问题。
- 将版本冲突判断中的两个 `baseUpdatedAt` 参数显式转换为 `BIGINT`，避免毫秒级版本号作为第 7 个 SQL 参数时再次触发 `22003: out of range for type integer`。
- 保留 v1.16.4 的旧列 `updated_at INTEGER → BIGINT` 自动迁移和保存重试；本次补丁确保迁移后的重试能真正完成。

## v1.16.6：自动三方合并 + 云端版本基线修复

- 修复 `fetchExamsFromServer` 中云端版本缓存写入位于 `return` 后、实际从未执行的问题；现在每次成功读取与保存都会写入云端版本及完整快照。
- 新增 `exam_cloud_snapshot` 作为最近一次成功同步的共同基线。保存遇到 409 冲突时，客户端自动使用「基线 / 本机待保存 / 云端新版本」执行三方合并，并以云端最新版本自动重试保存。
- 大型考试、分考试科目和带 `id` 的提醒项按 `id` 合并：两端各自新增的内容都会保留；仅一端修改的字段采用该端值；同一字段被两端同时改成不同值时保留本机当前编辑，避免静默丢失。
- 若自动合并后的重试期间云端再次变化，合并结果会保留在本机并提示稍后再保存，而不会覆盖远端数据。

## v1.17.5：完整提醒文字适配与五主题字体实现

- 新增 `useFitText`：利用 `ResizeObserver` 测量真实渲染宽度，提醒标题、主视觉、科目行、日期和说明文字超过可用宽度时按实际比例横向缩放，保持单行且不截断。
- 新增 `FitText` 通用组件，五套提醒布局统一接入动态文字测量；字体宽度受 Windows 缩放、屏幕尺寸或文本长度影响时会重新计算。
- 五套常驻大屏设计实际写入独立字体规则：指挥舱科技窄体、清爽聚焦现代 UI、校园黑板厚重展示、高对比应急粗体、编辑排版衬线/编辑风；所有字体栈保留中文系统回退以避免自定义科目缺字。
- 设计三提醒中心 Grid、矮屏收紧与单行测量共同生效，修复超大中文科目或“考试结束”场景的换行、底部挤压和视觉失衡。

## v1.17.4：全设计提醒字体与大屏布局优化

- 五套提醒主题统一加入单行排版保护：标题、科目、时间和底部说明禁止中文自动换行，按可用宽度和屏幕高度使用响应式字号，减少 Windows 缩放与超长文本造成的重叠。
- 五套设计分别使用更鲜明的本机字体栈：指挥舱采用窄体科技风、清爽聚焦采用现代 UI 风、高对比应急采用粗体高可读风、编辑排版采用衬线编辑风、校园黑板采用厚重中文展示风；字体不可用时稳定回退到中文系统字体。
- 修复设计三（校园黑板）提醒布局：中心区域改为稳定 Grid，主标题、科目、分隔线与说明各自占位；超矮屏自动收紧间距和缩放，避免底部文案被挤压。

## v1.17.3：完整离线应用壳 + 时间线与首页优化

- **离线刷新保障**：Service Worker 安装时解析并预缓存当前 `index.html` 引用的同源 JavaScript、CSS、图标和 manifest；保留上一份已验证应用壳，新版本资源未完成前不会淘汰旧缓存。离线刷新优先使用当前壳，失败时回退上一个可用壳。
- **本地数据库迁移**：首次读取旧 `localStorage` 的 `AppSettings` 后自动镜像迁移至 IndexedDB；原 localStorage 继续保留作同步兼容与读取回退，迁移失败不会影响旧数据。
- **冲突记录增强**：三方合并检测到同字段并发修改时，除保留本机编辑外同步把冲突数量、本机载荷和云端版本写入 IndexedDB 冲突历史；后台成功合并后明确提示已保留本机值。
- **考试顺序完全按时间**：按开考、结束时间和科目 ID 稳定排序，保存时重新生成兼容编号；后台移除上下移动控件，新增、编辑、导入和恢复后始终自动按真实时间线排列。
- **后台启用逻辑修复**：按钮改为表示下一步动作——启用中的科目显示“停用”，已禁用科目显示“启用”，并提供明确影响说明。
- **Welcome 视觉优化**：重做首页深色渐变层次、主倒计���卡���、移动端安全区和窄屏布局，强化当前/下一场信息与进入大屏入口。
- 桌面端（Tauri + SQLite + 托盘通知）列入后续版本计划，本版不包含桌面安装包。

## v1.17.2：移动端菜单与更新提示修复

- **修复手机端“⋯ 更多”菜单溢出**：菜单打开时按触发按钮和可视视口动态计算固定定位、左边界和宽度，不再被窄屏、折行头部或横向布局裁切到屏幕外；导入/导出按钮触摸高度提升至 44px。
- **修复 PWA 更新提示遮挡**：手机端更新提示改为避开底部“立即同步”浮动控制区与系统安全区，保留“立即更新”操作；桌面端位置不变。

## v1.17.1：考试时间线排序修复 + 离线/PWA 可靠性增强

- **修复分考试顺序 bug**：考试展示、首页横幅、倒计时、当前/下一场判断和提醒上下文统一以 `startTime` 为主排序，`order` 仅在同一开考时间时作为次级顺序。后台新建较早时间的科目即使追加到列表底部，也会自动成为正确的第一场，不再导致首页或大屏选错/漏显示考试。
- 新增 `examSchedule` 公共时间线工具；本地设置规范化、后台新增/编辑/导入、首页与大屏均复用同一规则。后台不同开考时间的上下箭头自动禁用并说明原因；同一开考时间的科目仍可手动排序。
- **离线 outbox 重试增强**：`exam_pending_sync` 记录失败次数、上次错误和下次重试时刻；联网后按 5 秒、15 秒、30 秒、最长 60 秒的退避节奏自动重试。手动“立即同步”可跳过等待并立刻重试，失败始终保留本地数据。
- 首页新增本地变更、跨窗口、获得焦点和 PWA 恢复监听，后台离线编辑后首页考试横幅与倒计时立即更新。
- **PWA 新版本提示**：Service Worker 缓存更新至 v1.17.1；发现新版时显示“立即更新”，只刷新应用代码，不清除本地考试安排或待同步离线修改。

## v1.17.0：离线本地优先同步 + 大屏手动同步 + 下一科修复

- **离线本地即时展示**：`AppSettings` 每次写入后派发本地变更事件；大屏同时订阅同页事件与跨窗口 `storage` 事件。后台离线新增、编辑、切换考试后，同一设备/同一浏览器资料下正在运行的大屏会立即读取本机最新安排，无需联网或刷新。
- **持久离线 outbox**：新增 `exam_pending_sync`，保存完整本地载荷和编辑前云端基线。离线编辑后即使后台关闭或 PWA 被系统回收，恢复网络、回到前台、重新进入大屏/后台或手动同步时仍会自动上传；云端并发变更继续三方合并，旧云端数据不会覆盖待上传本地修改。
- **大屏同步入口**：新增全局悬浮“立即同步 / 载入本地数据”按钮及数据状态。离线时立即重载本地；在线时冲刷待办并检查云端；失败时保留本地数据并可重试，不执行整页刷新。
- **下一科提醒修复**：五套设计共用的下一科全屏提醒，主标题与底部占位符现在使用下一科；下一科跨天时自动显示日期，科目行显示「科目 · 日期 时间 开考」。
- **后台防误录与手机整理**：分考试时长超过 6 小时时要求确认跨天/特殊安排；手机端将导入/导出 JSON 收纳到“⋯ 更多”，高频的提醒、公告、设置保持直接可见。

## v1.16.9：考试时间更新后的提醒补全 + 前台恢复同步

- 补全 v1.16.8 的提醒版本 key：内置提醒 key 由仅含 `startTime` 改为同时含 `startTime` 和 `endTime`（`${id}_${phase}_${startTime}_${endTime}`）。管理员仅延长/缩短考试结束时间时，结束前 15 分钟和考试结束提醒也会按新时间重新计算，不会被旧 `fired` 记录拦截。
- 修复考试数据更新过程中旧提醒可能卡住的问题：`useExamNotify` 的 effect 清理阶段现在会清除自动关闭 timer、重置 timer 引用并清空旧 notification，避免旧时间生成的浮层在新提醒未进入触发窗口时失去自动关闭机制。
- 提醒扫描依赖加入科目名称，管理端仅修改科目名称后，后续提醒文案也会使用最新名称。
- 同步补强：`useExamSync` 新增窗口 `focus` 与 `pageshow` 立即拉取。浏览器将后台标签页定时器节流/暂停，或从 BFCache 恢复时，回到前台即可拉取最新数据并重新检查提醒；原有首次加载、30 秒轮询、联网恢复和可见性恢复同步保留。
- 不包含旧浏览器 legacy bundle 适配；已经运行旧版 JS 的页面仍需刷新或重新进入，才能获得新版提醒代码。

## v1.16.8：管理员改时间后客户端自动重新触发全屏提醒

- 修复管理后台修改考试时间后，客户端不刷新页面时全屏提醒不再弹出的问题。
- **根本原因**：提醒触发记录（`fired` / `firedRef`）的 key 只含科目 ID，时间改变但 ID 不变��，旧记录拦截了新提醒；`useExamNotify` 的 effect 依赖也只含 `exam?.id`，时间变了 effect 不重跑，闭包仍用旧时间计算触发点。
- **修复**：① 内置提醒 key 改为包含 `startTime`（`${id}_${phase}_${startTime}`），时间变即为全新 key，旧记录不再拦截；② effect 依赖改为 `[exam?.id, exam?.startTime, exam?.endTime]`，时间变更后 effect 重跑，闭包拿到最新时间；③ `useAlertOverlay` 自定义提醒 key 同步加入 `startTime` + `endTime`。三处协同：管理员改时间 → 30 秒同步生效 → 处于触发窗口内（距触发点 ≤60 秒）的提醒自动重新弹出，无需刷新页面。

## v1.16.7：跨天倒计时格式 + 候考阶段“下一科目”修复

- 修复考试大屏倒计时把跨天时长全部折算成小时的问题（例如距首场还有 108 天被显示成 `2606:16:10`）。现在超过 24 小时的倒计时统一显示为「N天 HH:mm:ss」，与 Welcome 首页保持一致；当天内仍为 `HH:mm:ss`。影响候考（距开考）与结束后等下一场两种场景，五套设计通用。
- 修复候考阶段（首场开考前）“下一科目”错误显示为第二科的问题。此前倒计时对准的是第一科，但“下一科目”卡片却跳过第一科直接显示第二科，造成时间与科目对不上。现在候考阶段不再展示“下一科目”，仅保留对准即将开考科目��倒计时与本场信息；“下一科目”只在考试进行中出现，指向真正的下一场。

## v1.18.0–v1.18.8：设计、字体、同步与稳定性更新

- **13 套可切换设计**：保留原有五套设计，新增晨光日程、色卡仪表盘、圆环聚焦、蜜桃任务板、海报网格、冰蓝分栏，以及两套暗色设计「霓虹石英」「影院红线」；设计 15 未加入。新增设计与既有提醒均使用真实考试视图数据，并针对桌面和窄屏保持单行、动态缩放与安全区适配。
- **全屏提醒扩展**：09–14 亮色设计和 16–17 暗色设计均已接入开考前 15/5 分钟、开考、结束前 15 分钟、结束、下一科六种全屏提醒状态；提醒字体、标题、内容与倒计时遵循统一的四分区字体规则。
- **本地字体与设置**：字体资源随部署包提供，不依赖客户端本机字体。设置页可分别调整「导航与标签、展示标题、动态内容、时钟与数字」四个区域；数字区域默认使用本地 General Sans，霞鹜文楷仅作为可选字体而非默认字体。
- **同步与连接优化**：`/api/exams` 使用 ETag、10 秒 Edge/���缓存与本地快照回退，降低重复数据库读取；部署建议让 Vercel Functions 与 Neon 位于美国同一区域。更新后会立即清空热缓存。
- **时间稳定性**：网络校时采用三路并行采样、低 RTT 样本中位数、同源 Vercel `Date` 响应兜底；失败时保留最近一次成功偏移，网络恢复或页面回到前台自动复校，确保断网和跨洲波动下时钟持续稳定。
- **作者水印**：大屏桌面端右下角显示 `Created by PikaNova`；手机端（≤620px）自动隐藏，避免与同步入口重叠。

## v1.20.2 客户端体验与展示稳定性

- 首页按等待、考试中、暂无考试状态分别使用蓝、绿、灰状态区，并调整主操作、科目说明和倒计时。
- 新增大屏“更多操作”菜单，提供切换设计、返回首页和进入后台；返回与后台操作均有防误触确认。
- 13 套设计与对应全屏提醒统一加入窄屏、超窄屏、矮屏、长文本和视口溢出保护；标题、科目、状态、数字和通知条保持稳定布局，时钟使用等宽数字。
- 设计切换器加入环境标签、收藏记录、键盘方向键切换、Enter/Esc 操作与可见焦点。
- 设置页字体分区加入即时预览和恢复设计默认；后台增加启用科目时间重叠确认。
- 全站遵从 `prefers-reduced-motion`，降低系统要求减少动态时的动画与过渡。

## 作者遥测台 v1.8.0

- 公告支持搜索、草稿/发布筛选、批量展开/收起、Markdown 复制、未保存修改提醒和客户端样式预览。
- 公告数据新增 `publish_at` / `unpublish_at`；公开接口自动隐藏未到发布时间或已过下线时间的公告。
- 性能页支持 24 小时、7 天、全部样本筛选，按域名与网络类型筛选，并展示 P50/P95 与慢加载标记。
- 作者端手机布局优化：导航横向滚动、指标卡双列、筛选区换行、设备信息截断保护、表格容器内横向滚动。

> 发布顺序：先部署作者遥测台，再部署考试看板。两者的公告与性能采集接口保持兼容。

## v1.21.0：首页最近打开 + 后台撤销删除/列表折叠 + 静默提醒
### 客户端
- **首页「上次打开」**：首页在副标题下显示上一次打开看板的时间（本机记录，不上传）。
- **后台撤销删除**：删除分考试后出现「撤销删除」条，可一键恢复到原位置，避免误删。
- **列表折叠**：分考试列表支持「折叠/展开列表」，科目较多时后台更清爽（桌面端与移动端均适配）。
- **静默提醒模式**（设置 → 提醒与高级）：全部提醒（默认）/ 仅关键提醒（5 分钟 / 开考 / 结束 / 下一科）/ 本场进行中暂停提醒（考试进行期间不打断，结束后恢复）。

## v1.9.0（作者端 exam-board-telemetry）：定时公告 + 性能深化
- **定时公告**：公告编辑器新增「定时发布 / 自动下线」时间（`publish_at` / `unpublish_at`），到点自动对客户端可见或隐藏；列表状态区分 已发布 / 定时中 / 已下线 / 草稿。
- **公告操作增强**：复制 Markdown / 复制纯文本双模式；删除后可「撤销删除」重建；列表筛选新增「仅置顶」「定时中」。
- **性能页深化**：新增「近 30 天」范围、地区与客户端版本筛选；指标补充 P90；自动「性能异常」提示；支持导出筛选样本 JSON。

## v1.21.1 — 设计显示修复

针对 13 个展示设计的一批显示问题修复：

- **数字等宽**：新增 `src/components/Digits.tsx` 组件，将时钟/时间中的每个数字包进固定宽度 span（不依赖字体自带的 tabular-nums，子集字体常缺失该特性）。已应用到全部设计的主时钟、编辑排版秒数、蜜桃/冰蓝侧栏竖排数字、海报/日出拆分时钟等，数字跳动时不再左右晃动。
- **移除右上角三点菜单**：删除了浮层 `ExamQuickMenu`（所有设计右上角的「···」快捷菜单），保留各设计原有的公告/切换/管理按钮。
- **霓虹石英 / 影院红线 顶部功能栏不可点**：根因是 `.nd-head` 缺少层叠上下文，`.nd-main` 内容在首帧溢出覆盖了表头按钮（改变窗口触发重排后才恢复）。为 `.nd-head` 增加 `position:relative;z-index:3`，与浅色设计 `.ld-head` 一致；并让 `useFitText` 在 `document.fonts.ready` 后重新测量适配。
- **左侧时间数字间隔太小**：放宽蜜桃/冰蓝侧栏竖排数字的 `line-height` 与字距，`.ld-clock` 负字距收敛到接近 0。
- **海报网格错位**：`.ld-poster-side` 由 3 列改为 2 列（`repeat(2,minmax(0,1fr))`），消除某些窗口宽度下第三列空缺导致的排版错位。

## v1.21.5 — 全屏提醒字体修复（考试提醒 + 公告提醒）

- 修复全屏考试提醒大字在文案态（如「考试结束」「本场考试结束」）使用回退字体的问题：`exam-alert-overlay.css` 中 `.eao-hero` 此前用 `!important` 强制为仅含数字的 `--font-region-numeric`，导致中文不在数字子集内、掉到系统回退字体。现改为 `var(--font-region-numeric),var(--font-region-display)` 逐字回退：数字仍用等宽 JetBrains Mono，中文文案落到设计展示标题字体。此修复覆盖全部 13 套设计的全屏考试提醒。
- 修复全屏公告提醒（`exam-announcement-overlay.css`）未接入字体分区变量的问题：`.eann-window` 基础字体接 `--font-region-content`，`.eann-window__title` 与公告正文各级标题 `.md-h` 接 `--font-region-display`，正文内容继承内容字体。
- 内联公告列表（`announcement-list.css`）非全屏提醒，且已在 v1.21.4 通过 `body` 基础字体自动继承内容字体，无需改动。

## v1.21.4 — 字体设置全局统一（首页/后台/登录/公告/切换器）

- 修复「后台切换字体后首页刷新也不生效」：首页 `welcome.css` 此前把字体写死为 `system-ui,sans-serif`、完全没有接入字体分区变量，因此无论选什么、刷不刷新都不会变。现已接入——首页基础文字用 `--font-region-content`、大标题用 `--font-region-display`、倒计时与秒数用 `--font-region-numeric`。
- 字体设置全局统一：在 `main.tsx` 为 `body` 设置基础字体 `--font-region-content`，使此前未接入的界面（登录页、首启同意门、公告浮层/列表、PWA 更新提示等）自动继承用户所选字体；并替换后台管理页（`admin.css`）、设计切换器（`design-switcher.css`）、登录页（`login.css`）、同意门（`consent.css`）中写死的 `system-ui` / Microsoft YaHei 字体。
- 13 套设计经核对本就全部接入 4 个字体分区变量（导航/标题/内容/数字），本次保持不变。
- 仅保留大屏浮动同步状态胶囊、PWA 更新提示条与右下角作者水印为系统字体（属常驻微交互/设计固定项）。

## v1.21.3 — 字体后台默认 & 界面元素约束

承接 v1.21.2，修复「后台字体设置未生效」并为各字体加界面约束：

- **时钟数字默认 = JetBrains Mono**：设置页「④ 时钟与数字」新增 `JetBrains Mono（默认 · 等宽）` 选项并设为默认；应用运行时会把 `--font-region-numeric` 按该默认写入，因此现在打开即为等宽 JetBrains Mono（此前 `--font-numeric` 的改动被设置项的 `--font-region-numeric` 覆盖，故未生效）。原 `General Sans` 仍在列表中可选。
- **各字体界面元素约束（避免遮挡）**：所有大屏时钟改用已有的 `FitText`（按真实宽度测量后 `scaleX` 自适应，字体加载完成与窗口尺寸变化时重新计算），无论选用哪种字体，时钟都会缩放到容器内，不再溢出遮挡右侧卡片/侧栏。蜜桃、冰蓝侧栏与海报数字块补充 `overflow` 裁剪约束作为兜底。

## v1.21.2 — 数字等宽字体（JetBrains Mono）

承接 v1.21.1 的数字显示修复，改用真·等宽字体代替之前的固定宽度 `Digits` 方案（后者会把数字撞得过宽）：

- **新增 JetBrains Mono 数字子集**：从官方 TTF 子集化出 Regular / SemiBold / ExtraBold 三个权重的 WOFF（仅包含 `0-9 : . - / % , +` 等时钟必需字符，每份约 8.5KB），本地打包于 `public/fonts/jetbrains-mono-*-subset.woff`，不依赖任何字体 CDN 或客户端本地字体。SIL OFL 1.1 许可，随包附带 `JetBrainsMono-OFL.txt`。
- **数字字体变量改为等宽栈**：`--font-numeric` / `--font-region-numeric` 主字体改为 `"Exam Mono Digit"`（JetBrains Mono），回退链依次为已内置的等宽子集与系统 monospace。数字天生等宽，不再依赖字体 tabular-nums 特性。
- **移除 `Digits` 组件及其包裹**：删除 `src/components/Digits.tsx` 与 `exam.css` 中的 `.dg-*` 撞宽规则，13 套设计的时钟/数字回归组件本身渲染，修复“数字太宽”。
- v1.21.1 的其他修复（移除右上角三点菜单、霓虹石英/影院红线顶栏 z-index、蜜桃/冰蓝竖排行距、海报侧栏两列）均保留。
